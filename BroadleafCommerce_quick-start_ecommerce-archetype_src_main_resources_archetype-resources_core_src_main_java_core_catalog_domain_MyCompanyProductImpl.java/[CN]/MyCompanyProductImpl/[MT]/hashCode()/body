{
  final int prime=31;
  int result=super.hashCode();
  result=prime * result + (restricted ? 1231 : 1237);
  result=prime * result + ((shippingCountries == null) ? 0 : shippingCountries.hashCode());
  return result;
}
