{
  if (clearCurrentView) {
    clearCurrentView();
  }
  ViewClass view;
  if (!containsKey(value)) {
    put(value,instance);
    view=instance;
  }
 else {
    view=get(value);
  }
  currentView.push(view);
  return view;
}
