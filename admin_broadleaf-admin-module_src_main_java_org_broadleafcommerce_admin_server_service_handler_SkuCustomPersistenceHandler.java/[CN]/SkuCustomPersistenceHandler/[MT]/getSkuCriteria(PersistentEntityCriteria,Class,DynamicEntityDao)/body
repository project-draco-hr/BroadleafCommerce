{
  Criteria hibernateCriteria=deDao.createCriteria(entityClass);
  hibernateCriteria.createAlias("product","prod",CriteriaSpecification.LEFT_JOIN).createAlias("prod.defaultSku","defaultSku",CriteriaSpecification.LEFT_JOIN);
  criteria.apply(hibernateCriteria);
  return hibernateCriteria;
}
