{
  Criteria hibernateCriteria=deDao.createCriteria(entityClass);
  hibernateCriteria.createAlias("product","product",CriteriaSpecification.LEFT_JOIN).createAlias("product.defaultSku","defaultSku",CriteriaSpecification.LEFT_JOIN);
  criteria.apply(hibernateCriteria);
  return hibernateCriteria;
}
