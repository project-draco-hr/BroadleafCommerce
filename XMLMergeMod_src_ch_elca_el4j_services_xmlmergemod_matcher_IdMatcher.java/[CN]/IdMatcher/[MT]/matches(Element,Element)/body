{
  return originalElement.getNodeName().equals(patchElement.getNodeName()) && originalElement.getAttribute("id") != null && patchElement.getAttribute("id") != null && originalElement.getAttribute("id").equals(patchElement.getAttribute("id"));
}
