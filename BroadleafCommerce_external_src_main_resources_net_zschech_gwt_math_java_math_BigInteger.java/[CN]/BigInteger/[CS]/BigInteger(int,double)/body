{
  this.sign=sign;
  if (val < POW32) {
    numberLength=1;
    digits=new int[]{twosComplement(val)};
  }
 else {
    numberLength=2;
    digits=new int[]{twosComplement(val % POW32),twosComplement(val / POW32)};
  }
}
