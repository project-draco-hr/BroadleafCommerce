{
  JdbcTemplate template=new JdbcTemplate(dataSource);
  int rowsAffected=template.update("UPDATE BLC_ORDER_LOCK SET LOCKED = ? WHERE ORDER_ID = ?","N",order.getId().toString());
  return rowsAffected == 1;
}
