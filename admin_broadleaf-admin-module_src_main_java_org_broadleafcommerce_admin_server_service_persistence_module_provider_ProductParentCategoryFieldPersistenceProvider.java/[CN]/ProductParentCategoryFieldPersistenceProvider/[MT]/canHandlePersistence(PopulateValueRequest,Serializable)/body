{
  Property property=populateValueRequest.getProperty();
  return instance instanceof Product && property.getName().equals("defaultCategory") && !ParentCategoryLegacyModeServiceImpl.getLegacyModeService().isLegacyMode();
}
