{
  response.setBufferSize(0);
  String query=request.getRequestURI();
  Matcher urlMatcher=urlPattern.matcher(query);
  if (!urlMatcher.matches())   return;
  for (int i=0; i < headers.length; i++)   response.setHeader(headers[i],values[i]);
}
