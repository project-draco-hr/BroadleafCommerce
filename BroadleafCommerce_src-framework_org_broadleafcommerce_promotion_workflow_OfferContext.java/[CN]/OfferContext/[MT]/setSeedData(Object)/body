{
  if (!(seedObject instanceof Order)) {
    log.error("STOPPING Workflow Process, seed data instance is incorrect. " + "Required class is " + Order.class.getName() + " "+ "but found class: "+ seedObject.getClass().getName());
    setStopEntireProcess(true);
  }
  seedData=(Order)seedObject;
}
