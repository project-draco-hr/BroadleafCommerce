{
  return new StringBuilder().append(getGlobalPrefix()).append(searchableFieldType.equals(FieldType.PRICE) ? getPricelistPrefix() : getLocalePrefix()).append(field.getPropertyName()).append("_").append(searchableFieldType.getType()).toString();
}
