{
synchronized (monitor()) {
    check_orphaned();
    noNamespace.RequestPackageV2Type target=null;
    target=(noNamespace.RequestPackageV2Type)get_store().find_element_user(PACKAGE$0,i);
    if (target == null) {
      throw new IndexOutOfBoundsException();
    }
    target.set(xpackage);
  }
}
