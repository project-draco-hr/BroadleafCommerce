{
  if (obj == null)   return false;
 else   if (!(obj instanceof CategoryProductXrefPK))   return false;
  return category.getId().equals(((CategoryProductXrefPK)obj).category.getId()) && product.getId().equals(((CategoryProductXrefPK)obj).product.getId());
}
