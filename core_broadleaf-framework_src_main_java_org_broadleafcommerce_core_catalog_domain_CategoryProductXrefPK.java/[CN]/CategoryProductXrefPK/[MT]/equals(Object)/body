{
  if (obj instanceof CategoryProductXrefPK) {
    CategoryProductXrefPK that=(CategoryProductXrefPK)obj;
    return new EqualsBuilder().append(category.getId(),that.category.getId()).append(product.getId(),that.product.getId()).build();
  }
  return false;
}
