{
  return BLCMapUtils.keyedMap(getProductAttributes(),new TypedClosure<String,ProductAttribute>(){
    @Override public String getKey(    ProductAttribute value){
      return value.getName();
    }
  }
);
}
