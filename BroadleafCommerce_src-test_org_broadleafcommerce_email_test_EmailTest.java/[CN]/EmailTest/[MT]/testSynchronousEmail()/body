{
  GreenMail greenMail=new GreenMail(new ServerSetup[]{new ServerSetup(30000,"127.0.0.1",ServerSetup.PROTOCOL_SMTP)});
  greenMail.start();
  emailService.sendTemplateEmail("to@localhost",new EmailInfo(),null);
  assert(greenMail.waitForIncomingEmail(10000,1));
  greenMail.stop();
}
