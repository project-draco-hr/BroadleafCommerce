{
  String ajaxParameter=request.getParameter(BLC_AJAX_PARAMETER);
  return (ajaxParameter != null && "true".equals(ajaxParameter)) || "XMLHttpRequest".equals(request.getHeader("X-Requested-With"));
}
