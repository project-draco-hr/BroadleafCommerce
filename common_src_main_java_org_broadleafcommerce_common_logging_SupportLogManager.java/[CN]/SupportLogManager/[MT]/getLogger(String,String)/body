{
  return (SupportLogger)getLoggerRepository().getLogger(name,new LoggerFactory(){
    @Override public Logger makeNewLoggerInstance(    String s){
      return new SupportLogger(moduleName,s);
    }
  }
);
}
