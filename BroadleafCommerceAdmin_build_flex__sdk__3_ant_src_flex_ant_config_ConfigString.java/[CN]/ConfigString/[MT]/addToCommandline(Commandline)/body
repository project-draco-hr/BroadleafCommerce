{
  if (value != null) {
    if (value.length() > 0) {
      cmdl.createArgument().setValue("-" + spec.getFullName() + "="+ value);
    }
 else {
      cmdl.createArgument().setValue("-" + spec.getFullName() + "=");
    }
  }
}
