{
  if (outString.matches(name)) {
    final String tmpValue=(fork ? "\"" + value + "\"" : value);
    outString.set(tmpValue);
  }
 else   if (icStrings.matches(name))   icStrings.addAll(value.split(" "));
 else   super.setDynamicAttribute(name,value);
}
