{
  if (urlPrefix != null) {
    urlPrefix=urlPrefix.trim();
    if ("".equals(urlPrefix)) {
      urlPrefix=null;
    }
 else     if (urlPrefix.equals(staticAssetUrlPrefix)) {
      urlPrefix=null;
    }
  }
  return urlPrefix;
}
