{
  AdminPresentationOverrides myOverrides=clazz.getAnnotation(AdminPresentationOverrides.class);
  return myOverrides != null && (!ArrayUtils.isEmpty(myOverrides.value()) || !ArrayUtils.isEmpty(myOverrides.toOneLookups()) || !ArrayUtils.isEmpty(myOverrides.dataDrivenEnums()));
}
