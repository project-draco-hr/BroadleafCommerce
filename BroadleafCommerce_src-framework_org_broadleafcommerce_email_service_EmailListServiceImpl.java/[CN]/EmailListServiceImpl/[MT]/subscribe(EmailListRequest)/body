{
  if (EmailListType.NONE.equals(emailListRequest.getEmailListType())) {
    return;
  }
  String profileId=null;
  String closestStore=null;
  try {
    Call call=generateCall();
    call.setOperationName(new QName("EmailManager","addEmailCustomer"));
    call.addParameter("in0",XMLType.XSD_STRING,ParameterMode.IN);
    call.addParameter("in1",XMLType.XSD_STRING,ParameterMode.IN);
    call.addParameter("in2",XMLType.XSD_STRING,ParameterMode.IN);
    call.addParameter("in3",XMLType.XSD_STRING,ParameterMode.IN);
    call.addParameter("in4",XMLType.XSD_STRING,ParameterMode.IN);
    call.addParameter("in5",XMLType.XSD_STRING,ParameterMode.IN);
    call.setReturnType(XMLType.XSD_STRING);
    Object[] args=new Object[]{emailListRequest.getEmailAddress(),"web",profileId,"Y",emailListRequest.getComment(),emailListRequest.getEmailListType().name()};
    String result=(String)call.invoke(args);
    if (emailListRequest.isSendConfirmationEmail()) {
      sendWelcomeEmail(emailListRequest.getEmailAddress());
    }
    logger.debug("subscribe() => Result: " + result);
    if (closestStore != null) {
    }
  }
 catch (  RemoteException e) {
    logger.error("subscribe () => Error subscribing '" + emailListRequest.getEmailAddress() + "': ",e);
  }
catch (  MalformedURLException e) {
    logger.error("subscribe () => Error subscribing '" + emailListRequest.getEmailAddress() + "': ",e);
  }
catch (  ServiceException e) {
    logger.error("subscribe () => Error subscribing '" + emailListRequest.getEmailAddress() + "': ",e);
  }
}
