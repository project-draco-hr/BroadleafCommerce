{
  Long date=null;
  if (includeTime) {
    date=getNow();
  }
 else {
    date=getToday();
  }
  if (startDate == null || startDate.getTime() > date || (endDate != null && endDate.getTime() < date)) {
    return false;
  }
  return true;
}
