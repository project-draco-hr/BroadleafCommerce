{
  if (skus == null) {
    skus=new ArrayList<Sku>();
    List<Sku> allSkus=getAllSkus();
    for (    Sku sku : allSkus) {
      if (sku.isActive()) {
        skus.add(sku);
      }
    }
  }
  return skus;
}
