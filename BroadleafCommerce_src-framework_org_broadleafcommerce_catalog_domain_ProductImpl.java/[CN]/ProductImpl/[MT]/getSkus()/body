{
  if (skus == null) {
    skus=new ArrayList<Sku>();
    List<Sku> skus=getAllSkus();
    for (    Sku sku : skus) {
      if (sku.isActive()) {
        skus.add(sku);
      }
    }
  }
  return skus;
}
