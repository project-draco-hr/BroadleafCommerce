{
  Date date=null;
  if (includeTime) {
    date=getNow();
  }
 else {
    date=getToday();
  }
  if (startDate == null || startDate.after(date) || (endDate != null && endDate.before(date))) {
    return false;
  }
  return true;
}
