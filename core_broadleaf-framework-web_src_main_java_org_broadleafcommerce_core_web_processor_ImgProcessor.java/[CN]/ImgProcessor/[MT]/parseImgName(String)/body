{
  String[] parts=imgName.split("/");
  if (parts.length <= 1) {
    return imgName;
  }
  for (  String name : parts) {
    if (name.contains(".")) {
      name=name.split("\\.")[0];
      return name.replace("-"," ");
    }
  }
  return null;
}
