{
  FilterAndSortCriteria[] fascs=form.getCriteria().toArray(new FilterAndSortCriteria[form.getCriteria().size()]);
  return getSectionPersistencePackageRequest(sectionClassName).withFilterAndSortCriteria(fascs).withStartIndex(form.getStartIndex());
}
