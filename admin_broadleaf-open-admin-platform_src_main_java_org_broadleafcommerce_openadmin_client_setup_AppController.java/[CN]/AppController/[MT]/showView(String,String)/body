{
  if (!BLCMain.ISNEW) {
    BLCMain.MODAL_PROGRESS.startProgress(new Timer(){
      public void run(){
        setupView(viewKey,presenterKey);
      }
    }
);
  }
 else {
    setupView(viewKey,presenterKey);
  }
}
