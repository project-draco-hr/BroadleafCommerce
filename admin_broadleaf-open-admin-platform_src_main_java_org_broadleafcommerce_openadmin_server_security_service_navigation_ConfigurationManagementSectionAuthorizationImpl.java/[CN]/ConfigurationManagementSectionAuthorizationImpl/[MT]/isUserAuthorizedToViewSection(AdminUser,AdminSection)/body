{
  try {
    return !section.getCeilingEntity().equals("org.broadleafcommerce.common.config.domain.AbstractModuleConfiguration") || dynamicEntityDao.getAllPolymorphicEntitiesFromCeiling(Class.forName("org.broadleafcommerce.common" + ".config.domain.AbstractModuleConfiguration")).length >= 1;
  }
 catch (  ClassNotFoundException e) {
    throw new RuntimeException(e);
  }
}
