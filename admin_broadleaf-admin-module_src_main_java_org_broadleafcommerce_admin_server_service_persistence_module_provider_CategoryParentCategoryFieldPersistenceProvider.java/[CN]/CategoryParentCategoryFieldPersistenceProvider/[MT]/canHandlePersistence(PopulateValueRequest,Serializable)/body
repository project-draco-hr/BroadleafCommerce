{
  Property property=populateValueRequest.getProperty();
  return instance instanceof Category && property.getName().equals("defaultParentCategory") && !ParentCategoryLegacyModeServiceImpl.getLegacyModeService().isLegacyMode();
}
