{
  int pos=-1;
  if (items.contains(item)) {
    pos=items.indexOf(item);
    items.remove(pos);
  }
  item.getAdapter().registerDataSourceSetupManager(this);
  if (destinationPos >= 0) {
    items.add(pos,item);
  }
 else {
    items.add(item);
  }
}
